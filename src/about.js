import React, { Component } from 'react';
import Layout from './layout';

export default class About extends Component {
  render() {
    return (
      <Layout>
        <div>Hola desde otra ruta!!</div>
      </Layout>
    );
  }
}
